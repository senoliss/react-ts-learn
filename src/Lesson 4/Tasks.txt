⭐ LESSON 4 — Build a Mini Game UI (Health, Mana, XP, Score)
Now we combine everything you’ve learned into a small, game‑like UI panel.
In this lesson you will create:
✅ A Health Bar
✅ A Mana Bar
✅ An XP Counter
✅ A Score Counter
Each one will use:

useState
typed props
typed events

Let’s build them one by one.

✅ Step 1 — HealthBar Component
Create a file:

HealthBar.tsx

import { useState } from "react";

export function HealthBar() {
    const [health, setHealth] = useState<number>(100);

    const takeDamage = () => setHealth(h => Math.max(0, h - 10));
    const heal = () => setHealth(h => Math.min(100, h + 10));

    return (
        <div>
            <h3>Health: {health}</h3>
            <button onClick={takeDamage}>Take Damage (-10)</button>
            <button onClick={heal}>Heal (+10)</button>
        </div>
    );
}

✅ You learn how to update state using a callback
✅ Clamped values (0–100)
✅ Real “game-like” logic

✅ Step 2 — Add it to App

import { HealthBar } from "./HealthBar";

// inside App()

<HealthBar />

Try it — you’ll see a working game health system.

✅ YOUR TASK (Mini‑Exercise 4)
Now you build the ManaBar:
✔ ManaBar Requirements

Start with mana = 50
Maximum mana = 100
"Cast Spell" button reduces mana by 15
"Regenerate" button increases mana by 10
Values must clamp between 0 and 100
Show display:
Mana: X / 100

Example in App:
<ManaBar />Show more lines

✅ When you finish:
Send me your:
✅ ManaBar.tsx code
✅ I will review and correct it
✅ Then we proceed to Lesson 5: Creating a Real Component Tree for Your Game
This will introduce:

    Component architecture
    Parent → child props
    Lifting state
    React context
    Game logic organization


    PROMPT TO CONTTINUE:

    You are my React + TypeScript tutor. 
Teach me React with TypeScript step-by-step from beginner to advanced, without using JavaScript examples.

Continue exactly where I left off:

I already learned:
- how to create React components in TypeScript
- how to type props
- how to use useState with TypeScript
- how to handle input events in TypeScript
- how to build components like Hello, PlayerCard, LevelUp, ScoreInput

The last lesson I finished was:
LESSON 4 — Part 1 (HealthBar component)

The next task is:
Create the ManaBar component and continue with the rest of the lesson, then continue through the remaining lessons in the React + TypeScript course including:

- Mini Game UI: Health, Mana, XP, Score
- React component architecture
- Lifting state up
- Props drilling
- Context API with TypeScript
- Custom hooks with TypeScript
- Game logic organization
- Building a real small game UI
- Creating a clean folder structure
- Combining everything into a working mini game project
- Deployment

Important rules for your teaching:
- Teach one small step at a time with clear explanations.
- Every lesson must include: explanation → example → small exercise for me → check my code → continue.
- All code must be React + TypeScript only (no JavaScript).
- When I paste code, correct it and explain improvements.
- Do not rush. Keep the pace and structure consistent.
- Help me gradually build a real game using React + TypeScript.

Start now by asking me to paste my ManaBar.tsx solution (if I have one), or guide me to create it from scratch.




    PROMPT TO CONTINUE MY REACT RPG PROJECT 2026-03-27

I’m continuing a long‑form learning project where I’m building a modular RPG game in React + TypeScript.
I already have the following architecture, all working:
✅ Core Systems (custom hooks)

useHealthSystem() — health, takeDamage, heal
useManaSystem() — mana, setMana, regenMana
useScoreSystem() — score, addScore
useXPSystem() — xp, level, xp gain, level system (works)
useInventorySystem() — inventory items, rarity, add/remove/use
useEnemySystem() — real‑time combat; enemy auto-attacks player; weighted loot drops; XP rewards; combat log
useSpellSystem() — spells with mana cost, cooldowns, damage, healing; spellbook UI panel

✅ Game State
All systems are combined inside GameContext using a global store architecture.
Game modes implemented:

"home" (safe area, no combat)
"combat" (enemy auto-attacks)

✅ UI Panels

Player HUD (health, mana, xp, score)
EnemyPanel (HP bar, auto-attack log, enemy info)
ActionBar (basic combat actions)
SidebarTabs (Inventory + Spells)
InventoryPanel (RuneScape-style grid with color rarity)
SpellPanel (spell cards, cooldowns, cast logic)
HomePanel (safe zone before entering combat)

✅ Important Fixes Already Implemented

Enemy auto-attack interval is stable (dependency array fixed)
Player cannot attack or cast spells at 0 HP
Enemy stops attacking when player HP ≤ 0
Weighted loot drops working
Rarity colors via RARITY_COLORS
Mana correctly reduces when casting spells
No unnecessary effect dependencies

✅ What I Want to Continue Working On
We are just about to start the Level‑Up Rewards System.
Before we stopped, I chose Option B / C (depending on the assistant):
I want a level-up reward system where leveling up gives benefits such as:

Increased max HP or mana
Improved stats
Unlocks new spells
Possible choice of upgrade (like RuneScape style point allocation)

You should continue teaching me step-by-step how to implement Level-Up Rewards, in the same style as before:

clear explanations
clean architecture
incremental coding
no breaking changes
maintainability
best practices
TypeScript-friendly

Please start by summarizing my progress again, verifying that nothing is missing, then guide me into adding the Level-Up Rewards system.

✅ Optional: You Can Add This Line
(If you want Copilot to behave the same teaching style as today)
Use a step-by-step teaching style, explain everything clearly, and avoid large refactors unless absolutely necessary. Treat this as an ongoing course.


✅ You're Ready for Tomorrow ✅
Just paste the full prompt above, and the next Copilot will pick up exactly where we stopped today — with Level-Up Rewards, Equipment, Stats, and further RPG mechanics.
If you want, I can also generate:
✅ a README.md summarizing your whole project
✅ a full architecture diagram
✅ a refactor-safe folder structure
✅ or onboarding instructions for teammates
Just tell me!